# Moongchichi 개발일지 v7
*작성: 2026년 8월 4일*
*이전 버전은 `Moongchichi_개발일지_v6.md` 참조*

---

## v7의 배경

v6에서 지어내기를 막고(웹 검색 + 검증), 엔티티 단위 중복을 막았다. 그런데 실제 발송분을 감사해보니 **받는 사람이 체감하는 문제는 다른 데 있었다.**

5/19~8/3 발송분 52일치를 전수 분석한 결과.

| 문제 | 실측 |
|---|---|
| 주제 패턴 반복 | 제목이 겹치는 쌍 **315건**. "홈·리빙 브랜드의 콘텐츠 전략"류가 9주 연속 |
| 마크다운 누출 | 실제 발송분에 `**` 2건, `#` 2건, 표 문법 1건 |
| 항목 길이 편차 | 최소 176자 ~ 최대 1,214자 |

v6의 엔티티 차단은 이 중 어느 것도 잡지 못한다. 반복되는 것이 고유명사가 아니라 **주제 구조**이기 때문이다.

---

## v7 작업 (8/4)

### 1차 — A·B·C 수정

| 문제 | 조치 |
|---|---|
| A. 주제 반복 | 브리핑이력에 `항목제목` 컬럼 추가. 최근 제목을 프롬프트에 넘겨 같은 구도 반복 금지 |
| B. 마크다운 | `strip_markdown` 신설. 발송 직전 제거. 구분선과 이모지는 보존 |
| C. 항목 길이 | 작성 규칙에 항목당 최소 600자 명시 |

10일 연쇄 시뮬레이션(앞 회차 결과를 다음 회차 차단 목록에 누적)으로 검증한 결과.

| 항목 | 결과 |
|---|---|
| 마크다운 | 10건 전부 0건 |
| 항목 길이 | 30개 항목 전부 764~1,055자 |
| 제목 중복 | 1쌍 (기준선 315쌍) |
| 웹검색 | 10건 모두 3회 실행 |

### 2차 — 원인 재분석

1차 검증에서 **수치 재사용**이 남았다.

- 무신사 "1조4,679억 원, 18.1% 성장, 영업이익 1,405억 원 36.7%"가 5일 간격으로 수치까지 동일하게 재등장
- 디 애퍼처 "상반기 매출 130% 증가" 문장이 6일 간격으로 거의 동일하게 재등장

처음에는 "수치를 저장해서 차단하자"고 접근했으나, 업종 분포를 세어보고 판단을 바꿨다.

```
패션        9일 / 10일  █████████   (최대 9일 연속)
음악        6일 / 10일  ██████
커리어      3일 / 10일  ███
IT테크      2일 / 10일  ██
홈리빙      1일 / 10일  █
```

**수치 재사용은 증상이고 원인은 업종 편중이었다.** 매일 같은 업종을 파니 그 업종의 뉴스 풀이 마르고, 같은 기사를 다시 읽게 된다. 수치를 막으면 다음엔 다른 패션 수치를 반복할 뿐이다.

편중이 생기는 이유는 구조적이었다. 프롬프트에 "관심 업종 60% / 인접 30% / 전체 10%"가 있는데, **항목이 3개라 60%면 매일 2개가 같은 업종으로 강제된다.**

과거 52일 감사의 "홈·리빙 9주 연속"도 같은 현상이다. 업종만 달랐다.

### 2차 — 수정 내용

| 항목 | 내용 |
|---|---|
| 비율 해석 | 하루 기준이 아니라 **최근 2주 전체 기준**으로 해석시킴. 오늘 관심 업종이 0개여도 됨 |
| 업종 연속 | 특정 업종이 최근 3일 연속 등장했다면 오늘은 제외 |
| 사실 재사용 | `항목제목` 열에 `제목 :: 핵심 문단`을 저장. 이미 인용한 수치·사례를 다음 생성이 인지 |
| 본문 보관 | `브리핑본문` 탭 신설. 전문을 따로 남김 |
| 규칙 재강조 | 소프트 차단 규칙이 긴 이름 목록에 파묻혀 위반된 사례가 있어 목록 뒤에 한 번 더 명시 |

**핵심 문단은 숫자가 가장 많은 문단을 고른다.** 배경 문단에는 수치가 거의 없다. 실제 데이터로 확인한 결과 무신사 수치 4개가 모두 다이제스트에 담겼다.

**LLM 호출과 시트 읽기를 늘리지 않았다.** 핵심 문단은 문자열 처리로 뽑고, 최근 제목은 기존 엔티티 조회 결과에 `_recent_titles` 키로 얹어 전달한다.

| 항목 | 변화 |
|---|---|
| 시트 읽기 | 3회 → **3회** |
| 시트 쓰기 | 1회 → **2회** (본문 탭) |
| LLM 호출 | 변화 없음 |
| 프롬프트 입력 | 약 +12,000 토큰 (2주치, 건당 34원 수준) |

**본문 탭을 분리한 이유.** 이력 탭은 브리핑마다 3회 읽힌다. 여기에 3,000자짜리 본문을 두면 매번 필요 없는 데이터를 통째로 끌고 오게 된다. 보관용과 투입용을 나눴다.

전체 본문을 프롬프트에 넣는 안도 검토했으나 기각했다. 2주치가 42,501 토큰(건당 123원)으로 비용은 감당 가능하지만, **프롬프트가 커지면 지시가 묻힌다.** 실제로 소프트 차단 규칙이 146개짜리 이름 목록 뒤에 파묻혀 위반된 사례가 있었다.

### 코드 정리

| 대상 | 조치 |
|---|---|
| 마크다운 방어 | 발송 경로 5곳 중 정기 브리핑만 적용돼 있던 것을 `/term`, 자연어 답변, 월간 요약까지 확대 |
| 노션 자동 아카이브 | `save_to_briefing_db` 등 호출부 없는 죽은 코드 삭제 (52줄) |
| `/topic` | 명령어 삭제 (172줄). 중복 코드와 미완성 상태가 함께 해소 |

---

## 배포·운영 점검 자동화

배포 성공 여부와 런타임 상태를 로컬에서 확인하도록 정비했다.

| 항목 | 내용 |
|---|---|
| Railway CLI | v5.30.4. 계정 토큰을 `RAILWAY_API_TOKEN` 사용자 환경변수로 등록 |
| 점검 스크립트 | `.claude/scripts/status.ps1` — git 동기화, GitHub 배포 상태, Railway 로그, 오류 라인 |
| 슬래시 명령 | `/status` |
| 토큰 마스킹 | httpx가 요청 URL을 통째로 남겨 텔레그램 봇 토큰이 로그에 노출됨. 파일 저장 전 치환 |

`.claude/`는 `.gitignore` 대상이라 커밋되지 않는다. **맥에서는 재구성이 필요하다.**

잔액 조회는 불가능함을 확인했다. Anthropic Usage/Cost API는 Admin API 키를 요구하고 개인 계정은 쓸 수 없다. 지출액만 조회 가능하며 잔액 엔드포인트는 존재하지 않는다.

---

## 맥 개발환경 세팅

의존성 전체의 `requires-python`을 확인한 결과 **`holidays 0.101`이 3.10 이상을 요구**한다. 맥 기본 파이썬 3.9.6으로는 설치가 실패한다. 나머지는 3.7~3.9면 된다.

```
brew install python@3.12
cd <repo> && git pull
/opt/homebrew/bin/python3.12 -m venv .venv        # 인텔 맥은 /usr/local/bin
.venv/bin/pip install -r requirements.txt
.venv/bin/python -m py_compile main.py services/*.py handlers/*.py
TELEGRAM_BOT_TOKEN=x ANTHROPIC_API_KEY=x GOOGLE_SHEETS_ID=x \
GOOGLE_SERVICE_ACCOUNT_JSON='{}' NOTION_API_KEY=x \
.venv/bin/python -c "import main, services.claude, services.sheets, services.briefing; print('import OK')"
```

주의할 점

- 윈도우 `.venv`는 맥에서 못 쓴다. 경로 구조(`Scripts/` vs `bin/`)와 바이너리가 다르다
- `.venv/`는 이미 `.gitignore`에 있다 (v6에서 미해결로 적혀 있던 항목, 해소됨)
- 자격 증명은 윈도우 사용자 환경변수에만 있다. 시트를 읽는 작업은 맥에서 다시 등록해야 한다
- `.claude/` 점검 도구는 PowerShell이라 맥에서 동작하지 않는다

---

## 명령어 목록

BotFather `/setcommands`에 등록할 내용. `/topic` 삭제 후 13개.

| 명령어 | 설명 |
|---|---|
| `/alarm` | 브리핑을 받는 시간을 원하는 시간으로 바꿀 수 있어요. |
| `/briefing` | 지금 바로 오늘의 브리핑을 받아볼 수 있어요. |
| `/distribution` | 콘텐츠 영역(트렌드/기본기/글로벌) 분포를 확인할 수 있어요. |
| `/feedback` | 불편한 점이나 개선 아이디어를 개발자에게 전달할 수 있어요. |
| `/help` | 사용할 수 있는 명령어 전체 목록을 볼 수 있어요. |
| `/pause` | 브리핑을 잠시 멈추고 싶을 때 사용해요. |
| `/profile` | 내 프로필과 브리핑 설정을 확인하고 수정 방법을 볼 수 있어요. |
| `/recap` | 날짜를 입력하면 그날 받은 브리핑을 다시 볼 수 있어요. |
| `/request` | 브리핑에 반영할 요청사항을 저장해요. 다음 브리핑부터 바로 반영돼요. |
| `/resume` | 멈춰둔 브리핑을 다시 받고 싶을 때 사용해요. |
| `/schedule` | 요일별 브리핑 내용을 확인할 수 있어요. |
| `/term` | 마케팅 용어가 헷갈릴 때 입력하면 쉽게 설명해드려요. |
| `/update` | 프로필 항목을 수정할 수 있어요. |

관리자 전용 `/reset`과 온보딩용 `/start`는 제외.

---

## 시트 구조

| 탭 | 용도 | 브리핑당 읽기 |
|---|---|---|
| 설정 | ChatID, 봇 상태, 노션 DB ID | 1회 이상 |
| 프로필 | 목표직무, 관심업종, 비율 | 1회 |
| 브리핑이력 | 날짜·테마·발송여부·소스링크·노션저장여부·주제키워드·영역분류·**엔티티**·**항목제목** | 3회 |
| **브리핑본문** | 날짜·테마·본문 전문 | **0회** (보관 전용) |

---

## 검증 상태 — 중단됨 (크레딧 소진)

2차 수정 후 10일 시뮬레이션을 재실행했으나 **3회차에서 API 크레딧이 바닥나 중단**됐다.

```
anthropic.BadRequestError: 400
Your credit balance is too low to access the Anthropic API.
```

2회차까지의 결과만 확보. 비교 기준.

| 지표 | 수정 전 | 목표 | 2회차까지 |
|---|---|---|---|
| 패션 업종 등장 | 10일 중 9일 (9일 연속) | 5일 이하 | 판단 불가 (표본 부족) |
| 동일 수치 재등장 | 2건 | 0건 | 0건 |
| 동일 사례 문장 재사용 | 1건 | 0건 | 0건 |
| 제목 중복 | 1쌍 | 유지 | 0쌍 |
| 마크다운 / 600자 미만 | 0건 / 0개 | 유지 | 0건 / 0개 |

2회차까지는 핵심 문단이 정상적으로 저장·투입됐고 수치 재사용도 없었으나, **10일 누적 효과는 확인하지 못했다.** 충전 후 재실행 필요.

### 이번 작업에서 소비한 API

| 작업 | 브리핑 생성 |
|---|---|
| 백필 엔티티 추출 | 52회 (CLAUDE_FAST_MODEL) |
| 단건 검증 | 1건 |
| 10일 시뮬레이션 1차 (프로세스 종료로 중단) | 1건 |
| 10일 시뮬레이션 2차 (완료) | 10건 |
| 10일 시뮬레이션 3차 (크레딧 소진) | 2건 |

웹검색이 붙은 브리핑 생성 **14건**. 건당 150~250원 추산으로 2,000~3,500원 규모.

**교훈: 잔액을 모르는 상태에서 연속 생성을 돌리면 안 된다.** 1차 결과가 나온 시점에 남은 예산을 확인했어야 했다. 잔액 조회 API가 없으므로 생성 횟수를 사전에 정하고 그 안에서 끝내는 방식이 필요하다.

---

## 미해결

- **API 크레딧 소진 (최우선)** — 충전하지 않으면 다음 브리핑 발송이 실패한다. 생성 단계에서 400이 나고 관리자에게 실패 알림만 간다
- **2차 수정 검증 미완료** — 크레딧 소진으로 3회차에서 중단. 충전 후 재실행 필요
- 백필된 엔티티는 이 기능 적용 이전 발송분 기준이라 `항목제목`에 핵심 문단이 없다. 실제 효과는 며칠 운영해야 쌓인다
- B안(슬롯 로테이션, 소재축 컬럼)이 여전히 필요한지 미정. 2차 수정으로 업종 편중이 잡히면 불필요할 수 있다
- `explain_term`(`/term`)과 `answer_natural_language`에 웹 검색 미적용. 4C 오류가 이 경로에서 발생
- `generate_monthly_summary`에 원본에 없는 정보 추가 금지 문구 미적용
- 7월 23일 미발송 원인 미상
- T/F/G 영역 분류 정확도 미검증
- 배포 환경 파이썬 버전 미고정 (`runtime.txt` 등 없음)
- 4월~5월 중순 17일치는 텔레그램 대화 삭제로 본문 복구 불가
- `services/claude.py` 800줄대. 프롬프트 문자열 분리 검토
- 함수 내부 `import` 8곳 (`sheets.py`의 `import datetime` 5회)

---

## v7에서 얻은 교훈

- **증상과 원인을 구분해야 한다.** 수치 재사용을 수치 차단으로 막으려 했으나, 세어보니 업종 편중이 원인이었다. 데이터를 먼저 세면 엉뚱한 곳을 고치지 않는다
- **비율 설정이 다양성을 죽일 수 있다.** 관심 업종 60%는 좋은 의도였지만 항목이 3개라 매일 2개가 같은 업종으로 고정됐다. 비율을 어느 기간 기준으로 볼지까지 정해야 한다
- **프롬프트 지시만으로는 형식이 지켜지지 않는다.** 마크다운 금지를 다섯 번 반복해 적어둔 경로에서도 실제 발송분에 새어나갔다. 코드로 걷어내는 방어가 필요하다
- **차단을 강하게 걸면 모델은 우회한다.** 브랜드를 전면 금지했더니 "글로벌 플랫폼" 같은 익명 표현으로 바꿔 썼다. 금지와 함께 우회 금지도 적어야 한다
- **프롬프트가 길어지면 지시가 묻힌다.** 규칙이 146개짜리 목록 뒤에 있으면 지켜지지 않는다. 긴 목록 뒤에는 핵심 규칙을 한 번 더 쓴다
- **자동 검사기를 그대로 믿으면 안 된다.** 제목 유사도 검사가 일반어를 불용어 처리하지 않아 오탐을 반복했다. 결과는 본문을 읽고 판정해야 한다
- **분류가 원리적으로 불가능한 경계는 코드로 나누지 말고, 틀려도 손해가 작도록 규칙 강도를 조정한다.** 매체와 브랜드는 구글·메타처럼 둘 다인 경우가 있어 소프트 차단으로 바꿨다
- **안 쓰는 기능은 유지 비용만 남긴다.** `/topic`을 지우자 중복 코드와 미완성 상태가 함께 사라졌다
- **저장과 투입은 분리한다.** 원본은 넉넉히 남기고 프롬프트에는 필요한 만큼만 넣는다
