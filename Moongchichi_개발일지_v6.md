# Moongchichi 개발일지 v6
*초기 개발: 2026년 4월 22-23일*
*v2~v5: 2026년 4월 23일 ~ 5월 18일 (이전 일지 참조)*
*v6 1차: 2026년 7월 30-31일 (지어내기 차단 — 웹 검색 연동, 모델 전환)*
*v6 2차: 2026년 8월 3일 (개발환경 정비, CLAUDE.md 규칙화, 엔티티 단위 중복 차단)*
*v6 3차: 2026년 8월 4일 (배포 점검 자동화, 과거 이력 백필, 주제 반복 차단, 코드 정리)*

---

## 버전 히스토리
| 버전 | 날짜 | 내용 |
|---|---|---|
| v1 | 2026-04-22 ~ 23 | 초기 개발 및 배포 |
| v2 | 2026-04-23 ~ 24 | 실사용 피드백 기반 폴리싱 |
| v3 | 2026-04-24 | pytz + datetime.time 타임존 오류 수정 |
| v4 | 2026-04-24 ~ 25 | 스케줄러 run_repeating 전면 교체, /alarm 즉시 반영 |
| v5 | 2026-05-18 | 미러 발송, 키워드 중복 방지 재설계, 3영역 콘텐츠 풀, 자동 영역 분류 |
| v6 | 2026-07-30 ~ 08-04 | 지어내기 차단(웹 검색 연동 + 검증 + 재시도), Sonnet 5 전환, 공휴일 스킵, 로컬 검증환경 구축, CLAUDE.md 프로젝트 규칙화, 엔티티 단위 중복 차단(A안), 주제 반복 차단, 마크다운 방어, `/topic` 삭제 |

---

## v6의 배경 — 왜 이 작업이 필요했나

5/19~7/29 발송분 147개 항목을 전수 분석한 결과, 봇이 사실이 아닌 내용을 지속적으로 생성해온 것이 확인됨.

확인된 대표 사례
- 동일한 수치("2.3x")가 서로 무관한 12개 주제에 각각 다른 출처와 함께 등장
- 실존하지 않는 캠페인명 서술 (예: Apple Music "For All the Feels")
- 4C 프레임워크를 필립 코틀러가 만들었다고 서술 (실제로는 1990년 Robert Lauterborn)

근본 원인
- 봇이 웹 검색을 전혀 하지 않고 모델 기억만으로 브리핑을 생성
- 콘텐츠의 70%를 영역 A(최신 트렌드)에 배정 — 가장 검증이 어려운 영역

영향
- 수신자(마케팅 취업 준비생)가 이 내용을 이력서·면접에 인용했을 가능성이 있음
- 따라서 지어내기는 폴리싱 항목이 아니라 최우선 결함으로 취급함

---

## v6 1차 (7/30-31) — 푸시 완료

### 모델 및 SDK
| 항목 | 변경 |
|---|---|
| 메인 모델 | claude-sonnet-4-6 → claude-sonnet-5 |
| anthropic SDK | 0.40.0 → 0.75.0 |
| thinking | `{"type": "disabled"}` 명시 (Sonnet 5 기본값 충돌 회피) |
| max_tokens | 12000 / 8000 / 4000으로 상향 |
| 응답 파싱 | `_extract_text` 헬퍼 신설, 11개 호출부 전면 교체 |
| 타임아웃 | 180초, `max_retries=0` |

### 웹 검색 연동
- `web_search_20250305` 도구 적용 (브리핑 max_uses=3, 기타 2)
- 검색 실행 여부를 코드에서 검증. 0회면 최대 3회까지 재생성 시도
- 응답에 등장한 출처 URL이 실제 검색 결과에 있는지 대조 (진위 검사)
- 저품질 도메인 23개 차단 (커뮤니티 게시판, 개인 블로그, 채용 공고 사이트 등)
- 항목별 출처 검사 추가
- `strip_source_block` — 본문 말미 출처 목록 중복 제거

### 그 외
- 한국 공휴일 스킵 (`holidays==0.101`). 온보딩 안내에는 있었으나 실제로는 미구현이던 항목
- 수신자별 발송 실패 알림 (여자친구 쪽만 실패하는 경우 포함)
- 월간 요약 마크다운 제거
- 면책 문구 수정
- 말투 규칙 강화 — "~라고 해요" 식 전언체 남용 금지

### 커밋 순서
`484fc31 → de1e308 → 9d042a6 → 8d15d4d → f3b2d0c → a0d742a → 1561a6a → 8127843`

---

## v6 2차 (8/3) — 개발환경 및 A안

### 로컬 검증환경 구축

그동안 코드 검증 없이 푸시해왔고, 배포 후에야 오류를 발견하는 일이 반복됨. 이를 막기 위해 로컬 파이썬 환경을 구성.

| 환경 | 상태 |
|---|---|
| 윈도우 | Python 3.12.10 + `.venv` 구성 완료. import 검사 통과 |
| 맥 | Python 3.9.6뿐이라 미완성. `holidays==0.101`이 3.10 이상을 요구해 설치 실패 |

맥에서 남은 작업
- `brew install python@3.12` → `.venv` 재생성 → `pip install -r requirements.txt` → import 검사
- 맥의 `.gitignore`에 추가한 `.venv/` 한 줄이 커밋되지 않은 상태

검증 절차 (CLAUDE.md에 기재)
- 문법 검사: `python -m py_compile [수정 파일]`
- import 검사: 더미 환경변수를 넣고 `main, services.claude, services.sheets, services.briefing` 임포트

배포 환경 파이썬 버전은 레포에 고정돼 있지 않음. `runtime.txt`, `.python-version`, `nixpacks.toml` 모두 없어 Railway 기본값을 사용 중. 고정 여부는 미결.

### CLAUDE.md 프로젝트 규칙 추가 (커밋 1b4bfa0)

기존 영문 코딩 가이드라인은 유지하고 아래 섹션을 추가.
- 프로젝트 개요 및 미러 발송 구조
- 승인 없이 푸시 금지, `requirements.txt` 수정 금지, 시키지 않은 파일 수정 금지
- 작업 후 한국어 요약 + `git diff --stat` 보고
- 검증 절차 (윈도우/맥 각각의 명령)
- 과거 사고 지점 7개 (마크다운 금지, run_daily 금지, 콜백 chat_id, gspread str 변환, Sheets 호출 제한, 사용자 메시지에 임의 출력 추가 금지, 검색 검증 로직 보존)

### A안 — 엔티티 단위 중복 차단

문제
v5의 키워드 차단은 GA4·ROAS 같은 좁은 토큰만 추출하도록 튜닝돼 있어 브랜드·인물·이론 단위 반복을 잡지 못함.

실측 데이터
| 대상 | 반복 |
|---|---|
| 세스 고딘 | 10주간 6회 |
| 이케아 | 10회 |
| 뱅앤올룹슨 | 4회 |
| 퍼미션 마케팅 | 6/5, 7/10, 7/30 세 차례 |
| 7/7과 7/10 | 사흘 간격 반복 |

구현
| 항목 | 내용 |
|---|---|
| `claude.extract_entities` | 브리핑 본문에서 고유명사를 `유형:이름` 형식으로 추출. CLAUDE_FAST_MODEL, 검색 없음 |
| 유형 | 인물 / 이론 / 브랜드 / 캠페인 4가지 |
| 저장 | 브리핑이력 탭에 `엔티티` 컬럼 추가. `` | `` 구분. 헤더 없으면 `_sync_add_history`가 자동 추가 |
| `sheets.get_blocked_entities` | 최근 180일 이력에서 유형별 마지막 등장일 계산 |
| 차단 기간 | 인물 90일, 이론 90일, 브랜드 45일, 캠페인 180일 |
| 판정 방식 | 등장 횟수는 보지 않음. 마지막 등장일만 봄. 1회만 나와도 차단 |
| 프롬프트 반영 | `generate_briefing`이 `blocked_entities`를 받아 system 프롬프트에 명시 |
| 호출 위치 | `_generate_briefing_data`에서 로드 → 생성 → 발송 후 추출 → `add_history`에 저장 |

인물 등급 완화 (설계 변경)
- 최초 설계는 4유형 모두 "절대 언급 금지"였음
- 변경 이유: 같은 인물이라도 다른 이론을 다룰 수 있음. 세스 고딘의 퍼미션 마케팅을 한 번 다뤘다고 보라색 소까지 90일 잠기는 것은 과함
- 변경 내용: 이론·브랜드·캠페인은 절대 언급 금지 유지. 인물만 "항목의 중심 소재로 삼기 금지"로 하향. 근거로 이름을 한 번 스쳐 언급하는 것은 허용하되, 한 항목에서 두 문장 이상 쓰지 못하게 제한
- 기간은 90일 그대로 유지. 규칙 강도와 기간을 동시에 바꾸면 두 달 뒤 어느 쪽이 효과를 낸 건지 구분할 수 없기 때문
- 관찰 지점: 같은 인물이 다시 자주 등장하되 매번 다른 이론이면 완화 성공. 같은 이론이 또 나오면 이론 차단이 뚫린 것이므로 인물 등급을 되돌려야 함

적용 범위
- 정기 브리핑과 `/briefing`에만 적용. `/topic`은 이번 단계에서 제외 (B안에서 호출부 정리 시 함께 처리)
- `entities`는 기본값 `None`인 선택 인자라 기존 호출부는 그대로 동작
- 과거 이력 행에는 엔티티 값이 없음. 차단 목록은 이 기능 적용 이후 발송분부터 누적됨

검증
- 문법 검사 통과, 더미 환경변수 import 검사 통과
- `ENTITY_BLOCK_DAYS` 키 4개와 `extract_entities` 허용 유형 4개가 코드포인트 단위까지 일치함을 확인
- 가짜 시트 데이터로 차단 판정 로직 실행 검증 — 마지막 등장일 기준 판정, 180일 밖 기록 무시, 형식 불량 토큰 무시 모두 의도대로 동작

---

## v6 3차 (8/4) — 배포 점검 자동화, 백필, 주제 반복 차단

### 배포·운영 점검 자동화

배포 성공 여부와 런타임 상태를 매번 대시보드에 들어가 확인하던 것을 로컬에서 처리하도록 정비.

| 항목 | 내용 |
|---|---|
| Railway CLI | v5.30.4 설치. 계정 토큰을 `RAILWAY_API_TOKEN` 사용자 환경변수로 등록 |
| 점검 스크립트 | `.claude/scripts/status.ps1` — git 동기화, GitHub 배포 상태, Railway 런타임·빌드 로그, 로그 내 오류 라인을 한 번에 확인 |
| 슬래시 명령 | `.claude/commands/status.md` — `/status`로 실행 |
| 토큰 마스킹 | httpx가 요청 URL을 통째로 남기는 탓에 텔레그램 봇 토큰이 로그에 노출됨. 파일 저장 전 `bot<REDACTED>`로 치환 |

`.claude/`는 `.gitignore` 대상이라 이 파일들은 커밋되지 않음. 맥에서 쓰려면 별도 구성 필요.

잔액 조회는 불가능함을 확인. Anthropic Usage/Cost API는 Admin API 키를 요구하고 개인 계정은 사용할 수 없음. 지출액만 조회 가능하며 잔액을 돌려주는 엔드포인트는 존재하지 않음.

### 브랜드 차단 완화 — 전면 금지에서 중심 소재 금지로

문제
과거 52일치에서 엔티티를 추출해보니 브랜드로 뽑힌 137개 중 약 40개가 Warc·Nielsen·Adweek·eMarketer 같은 **출처 매체**였음. 이들을 전면 금지하면 45일간 해당 매체를 출처로 인용할 수 없게 되어, 시스템 프롬프트가 "좋은 예"로 지정한 Nielsen과 정면 충돌.

더 근본적인 문제는 구분이 원리적으로 불가능하다는 점. 구글·메타·애플·LinkedIn·스포티파이는 브랜드이면서 동시에 리포트 발행처임.

해결
매체 목록을 완벽하게 큐레이션하는 대신 차단 강도를 바꿈. 브랜드를 인물과 같은 소프트 차단으로 이동.

| 유형 | 변경 전 | 변경 후 |
|---|---|---|
| 이론 · 캠페인 | 전면 금지 | 전면 금지 (유지) |
| 브랜드 | 전면 금지 | **중심 소재 금지** (출처 인용·스치는 언급 허용) |
| 인물 | 중심 소재 금지 | 중심 소재 금지 (유지) |

이렇게 하면 매체를 브랜드로 잘못 분류해도 출처로는 계속 쓸 수 있어, 분류 오류가 치명적이지 않게 됨.

### 엔티티 추출 규칙 보강

실측으로 드러난 오분류를 프롬프트에서 차단.

- 출처 매체·리서치 기관은 브랜드로 추출 금지
- 이론은 고유한 이름이 붙은 것만. "B2C 콘텐츠 마케팅", "Z세대 콘텐츠 소비 행동" 같은 주제 서술은 이론이 아님
- 같은 대상은 한 표기로만. "필립 코틀러"와 "Philip Kotler"를 둘 다 넣지 않음
- 회사명을 인물로 넣지 않음

노이즈가 나왔던 날짜로 재추출해 효과를 확인함.

### 과거 이력 백필

텔레그램 대화 내보내기(5/19~8/3, 52일치)에서 본문을 복원해 엔티티를 소급 기록.

| 항목 | 값 |
|---|---|
| 대상 | 52일 (시트 행과 본문이 모두 존재하는 날) |
| 추출 | 344개 |
| 매체·노이즈 제외 후 | 276개 |
| 표기 통일·오분류 정리 후 | **272개** |
| 시트 기록 | 48행 (중복 날짜는 첫 행에만) |
| API 호출 | 읽기 1회 + 쓰기 1회 |

4월~5월 중순 17일치는 텔레그램 대화를 지운 이력이 있어 본문이 없음. 복구 불가.

### 발송분 52일 감사 — 받는 사람 기준 문제 3가지

| 문제 | 실측 |
|---|---|
| A. 주제 패턴 반복 | 제목이 겹치는 쌍 **315건**. "홈·리빙 브랜드의 콘텐츠 전략"류가 9주 연속 발송 |
| B. 마크다운 누출 | `**` 2건, `#` 헤더 2건, 표 문법 1건이 실제 발송분에 포함 |
| C. 항목 길이 편차 | 최소 176자 ~ 최대 1,214자. 176자는 배경·핵심·실무 포인트 구조가 성립 불가 |

A는 엔티티 차단으로 못 막는 종류였음. 반복되는 것이 고유명사가 아니라 **주제 구조**이기 때문. 기존 키워드 차단도 `extract_keywords`가 추상어를 의도적으로 제외하도록 튜닝돼 있어 그물을 빠져나감.

### A·B·C 수정

| 문제 | 조치 |
|---|---|
| A | 브리핑이력에 `항목제목` 컬럼 추가. 최근 28일 제목을 프롬프트에 넘겨 같은 구도 반복 금지. 시트 읽기를 늘리지 않으려고 엔티티 조회 결과에 `_recent_titles` 키로 얹어 전달 |
| B | `strip_markdown` 신설. 발송 직전 `**`·`#`·표 문법 제거. 구분선과 이모지는 보존 |
| C | 작성 규칙에 항목당 최소 600자 명시 |

부작용 대응: 차단된 브랜드를 피하려 "글로벌 플랫폼", "업계 관계자" 같은 익명 표현으로 우회하는 현상이 관찰되어 `[뭉뚱그리기 금지]` 규칙을 추가.

### 10일 연쇄 시뮬레이션 검증

앞 회차의 제목·엔티티를 다음 회차 차단 목록에 누적시키는 방식으로 10일 운영을 압축 재현. 시트에는 쓰지 않고 생성만 수행.

| 항목 | 결과 |
|---|---|
| 마크다운 | 10건 전부 0건 |
| 항목 길이 | 30개 항목 전부 764~1,055자. 600자 미만 0개 |
| 제목 중복 | 1쌍 (기준선 315쌍) |
| 뭉뚱그리기 | 검출 5건은 전부 부분 문자열 오탐. 실제 익명 표현은 1건 |
| 웹검색 | 10건 모두 3회 실행. 검증 실패로 인한 재생성 없음 |

### 코드 정리

| 대상 | 조치 |
|---|---|
| 마크다운 방어 | 발송 경로 5곳 중 정기 브리핑만 적용돼 있던 것을 `/term`, 자연어 답변, 월간 요약까지 확대 |
| 노션 자동 아카이브 | `save_to_briefing_db`, `_create_briefing_db`, `reset_briefing_db`, `_briefing_db_id`, `NOTION_BRIEFING_DATABASE_ID` 삭제. 호출부가 한 곳도 없던 죽은 코드. 저장된 브리핑 DB(💾 버튼)는 영향 없음 |
| `/topic` | 명령어 삭제. 핸들러, `generate_topic_briefing`, `is_marketing_topic`, 등록, 도움말·온보딩 안내, 그로 인해 불필요해진 import 전부 제거 (172줄 삭제) |

`/topic` 삭제로 정기 브리핑과 중복돼 있던 키워드·영역분류 추출 코드, 그리고 엔티티·제목을 저장하지 않던 미완성 상태가 함께 해소됨.

---

## 명령어 목록

`/topic` 삭제 후 기준. BotFather `/setcommands`에 등록할 내용.

| 명령어 | 설명 |
|---|---|
| `/alarm` | 브리핑을 받는 시간을 원하는 시간으로 바꿀 수 있어요. |
| `/briefing` | 지금 바로 오늘의 브리핑을 받아볼 수 있어요. |
| `/distribution` | 콘텐츠 영역(트렌드/기본기/글로벌) 분포를 확인할 수 있어요. |
| `/feedback` | 불편한 점이나 개선 아이디어를 개발자에게 전달할 수 있어요. |
| `/help` | 사용할 수 있는 명령어 전체 목록을 볼 수 있어요. |
| `/pause` | 브리핑을 잠시 멈추고 싶을 때 사용해요. |
| `/profile` | 내 프로필과 브리핑 설정을 확인하고 수정 방법을 볼 수 있어요. |
| `/recap` | 날짜를 입력하면 그날 받은 브리핑을 다시 볼 수 있어요. |
| `/request` | 브리핑에 반영할 요청사항을 저장해요. 다음 브리핑부터 바로 반영돼요. |
| `/resume` | 멈춰둔 브리핑을 다시 받고 싶을 때 사용해요. |
| `/schedule` | 요일별 브리핑 내용을 확인할 수 있어요. |
| `/term` | 마케팅 용어가 헷갈릴 때 입력하면 쉽게 설명해드려요. |
| `/update` | 프로필 항목을 수정할 수 있어요. |

관리자 전용 `/reset`과 온보딩용 `/start`는 목록에서 제외.

---

## 진행 예정

### B안 — 슬롯 로테이션
문제: 49일 중 35일에서 첫 항목이 홈리빙. 수요일과 금요일은 100%. 소재는 달라도 배치가 매일 같아 중복으로 느껴짐.

- `classify_topic_axis(sections)` — 항목별 소재축 분류. 축은 홈리빙/음악/IT테크/프레임워크고전/커리어/플랫폼알고리즘/글로벌/기타. 기존 T/F/G 영역 분류와는 별개 축이므로 둘 다 유지
- 브리핑이력 탭에 `소재축` 컬럼 추가
- `get_recent_axes(days=7)`
- 프롬프트 강제 규칙 3개: 같은 축 2개 이상 금지 / 최근 3일 연속 첫 항목을 차지한 축은 오늘 첫 번째에 금지 / 최근 7일 축 이력 참고해 분산
- `add_history` 인자가 늘어나므로 호출부 정리

3차에서 `항목제목` 컬럼과 최근 제목 프롬프트를 넣어 반복이 315쌍에서 1쌍으로 줄었음. B안이 여전히 필요한지는 며칠 운영 데이터를 본 뒤 판단.

### 수치·사례 재사용 (최우선)
차단이 **이름 단위**라 브랜드명을 피해도 이미 인용한 통계와 사례는 그대로 재사용됨. 10일 시뮬레이션에서 확인.

- 무신사 "1조4,679억 원, 18.1% 성장, 영업이익 1,405억 원 36.7%"이 5일 간격으로 수치까지 동일하게 재등장
- 디 애퍼처 "상반기 매출 130% 증가" 문장이 6일 간격으로 거의 동일하게 재등장
- 08-10 항목은 무신사·에이블리에 두 문장 이상을 써서 소프트 차단 규칙 자체를 위반

### 남은 지어내기 구멍
- `explain_term`(`/term`)과 `answer_natural_language`에 웹 검색 적용. 현재 검색 없이 답변하며, 4C 오류가 이 경로에서 발생
- 두 함수는 CLAUDE_FAST_MODEL을 쓰는데 `web_search_20250305` 지원 여부 확인 필요. 애매하면 CLAUDE_MAIN_MODEL로 상향
- `generate_monthly_summary` — 검색은 붙이지 않음(저장된 브리핑 요약 함수라 원본에 없는 내용을 넣는 것이 문제). system·user 양쪽에 원본에 없는 정보·수치·출처·고유명사 추가 금지 명시

### 코드 정리
- `services/claude.py`가 500줄 초과. 프롬프트 문자열을 `services/prompts.py`로 분리
- 응답 파싱 헬퍼를 별도 모듈로 분리
- 상수를 `config.py`로 통합
- 동작 변경 없이 위치만 이동
- `pytz`와 `notion-client` 사용 현황 확인만 — 개발일지에는 금지로 적혀 있으나 실제로는 사용 중
- 함수 내부 `import` 8곳 (`sheets.py`의 `import datetime`이 5회 반복). 실익이 적어 보류

3차에서 죽은 노션 코드와 `/topic`을 삭제해 254줄이 줄었음. `services/claude.py`는 여전히 800줄대.

---

## 미해결

- **수치·사례 재사용** — 위 진행 예정 참조. 현재 최우선
- 7월 23일 미발송 원인 미상. Railway 로그 확인 필요
- T/F/G 영역 분류 정확도 미검증
- 배포 환경 파이썬 버전 미고정
- 맥 로컬 검증환경 미완성
- `.claude/` 점검 스크립트가 윈도우 전용이고 gitignore 대상이라 맥에서 재구성 필요
- 4월~5월 중순 17일치는 텔레그램 대화 삭제로 본문 복구 불가. 엔티티 백필에서 제외됨

---

## v6에서 얻은 교훈

- 검증 없이 푸시하는 습관이 배포 후 장애로 이어졌다. 로컬 문법·import 검사는 비용이 거의 없고 사고를 크게 줄인다
- 지어내기는 폴리싱이 아니라 결함이다. 그럴듯한 문장은 오류를 감춘다
- 검색을 붙이는 것만으로는 부족하다. 검색이 실제로 실행됐는지 코드로 검증하고, 출처 URL이 실제 검색 결과에 있는지 대조해야 한다
- 키워드 단위 차단은 좁은 토큰만 잡는다. 반복은 브랜드·인물·이론 단위에서 일어난다
- 규칙 강도와 적용 기간을 동시에 바꾸면 나중에 원인을 분리할 수 없다. 한 번에 하나만 바꾼다
- 매번 반복해서 지시하게 되는 내용은 CLAUDE.md로 옮긴다. 작업별 지시는 프롬프트에 남긴다
- 로컬 환경 판단 시 한쪽 컴퓨터의 상황을 다른 쪽에 그대로 적용하면 불필요한 설치가 발생한다 (맥 3.9 → 윈도우에 3.13 설치 후 제거)
- 프롬프트 지시만으로는 형식이 지켜지지 않는다. 마크다운 금지를 프롬프트에 다섯 번 반복해 적어둔 경로에서도 실제 발송분에 새어나갔다. 코드로 걷어내는 방어가 필요하다
- 차단 규칙을 강하게 걸면 모델은 우회한다. 브랜드를 전면 금지했더니 "글로벌 플랫폼" 같은 익명 표현으로 바꿔 썼다. 금지와 함께 "이렇게 우회하지 마라"를 같이 적어야 한다
- 분류가 원리적으로 불가능한 경계를 코드로 나누려 하지 말고, 틀려도 손해가 작도록 규칙 강도를 조정하는 편이 낫다 (매체 vs 브랜드 → 소프트 차단)
- 자동 검사기의 판정을 그대로 믿으면 안 된다. 제목 유사도 검사가 "브랜드·전략·콘텐츠" 같은 일반어를 불용어 처리하지 않아 오탐을 반복해서 냈다. 결과는 본문을 읽고 판정해야 한다
- 안 쓰는 기능은 유지 비용만 남긴다. `/topic`을 지우자 중복 코드와 미완성 상태가 함께 사라졌다
- 배포 완료를 기다린다고 20초마다 폴링하면 API 한도를 태운다. 한 번 조회하고 필요하면 나중에 다시 본다
